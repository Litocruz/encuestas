<header class="navbar navbar-fixed-top">
<div class="navbar-inner">
  <div class="container">
    <%= link_to "DT", root_path, id: "logo", class: "brand" %>
    <nav>
      <% if !current_user.nil? %>
        <div class="btn-group pull-right">
          <a class="btn dropdown-toggle" href="#" data-toggle="dropdown"> <i class="icon-user"></i> <%= current_user.email %> <span class="caret"></span> </a>
          <ul class="dropdown-menu">
              <li class="divider"></li>
              <li><%= link_to "Salir", main_app.destroy_user_session_path, method: :delete %></li>
              <li><%= link_to "Registrar", new_registration_path(:user) %></li>
          </ul>
        </div>
      <% end %>
      <% if !current_user.nil? %>
        <ul class="nav pull-left">
          <li class="active"><%= link_to "Inicio", root_path %></li>
          <li><%# link_to "Surveys", rapidfire.question_groups_path %></li>       
          <li id="fat-menu" class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              Opciones <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Encuestas", rapidfire.question_groups_path %></li>
              <li><%= link_to "Nueva Encuesta", rapidfire.new_question_group_path %></li>
              <li class="divider"></li>
            </ul>
          </li>         
        </ul>
      <% else %>
       <ul class="nav pull-right">
            <li><%= link_to "Ingresar", main_app.new_user_session_path %></li>
        </ul>
      <% end %>
    </nav>
  </div>
</div>
</header>